# Module data from env.nu
source ~/.zoxide.nu

# Config section
$env.config = {
  # Disable welcome message
  show_banner: false
}

# Aliases
alias cls = clear
alias l = ls
alias ll = ls -la
alias la = ls -a
alias cd = z
